# Project Memory

## Project Location

- Workspace: `C:\lithium_news_system
- Current scan date: 2026-06-17
- Current observed state: no project files or subdirectories were present in this workspace when this memory file was generated.

## Current Context

This `MEMORY.md` was created as a starter project memory file. Because the workspace was empty at generation time, it intentionally avoids assumptions about frameworks, data sources, scripts, paper structure, dashboard implementation, or analysis workflow.

## Working Rules

- Keep future edits scoped to the files and sections requested by the user.
- Do not infer or rewrite data-processing logic without first inspecting the actual project files.
- If dashboard code is later added, preserve existing calculations, CSV/schema assumptions, and visual narratives unless the user explicitly asks for logic changes.
- If this project contains paper or thesis materials, keep wording precise, formal, and consistent with the document's existing terminology.
- Prefer Chinese for user-facing explanations unless the user requests English.

## How To Update This Memory

When project files are added, update this file with:

- Main entry points, such as dashboard scripts, notebooks, documents, or data folders.
- Commands for running, validating, exporting, or compiling the project.
- Important data files and schema assumptions.
- User-approved terminology, section names, chart labels, and presentation preferences.
- Known pitfalls, such as fragile file paths, encoding issues, required environment variables, or package-version constraints.

## Verification Notes

- `rg --files` could not be used in this environment because `rg.exe` was denied by Windows.
- PowerShell recursive directory scans found no files or folders under the current workspace before this file was created.
